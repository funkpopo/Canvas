server:
  host: "0.0.0.0"
  port: 8000

database:
  type: "mysql"
  sqlite_db_path: "backend/canvas.db"
  host: "mysql"
  port: 3306
  name: "canvas"
  user: "canvas"
  password: "CHANGE_ME"

auth:
  jwt_secret_key: "CHANGE_ME"
  jwt_algorithm: "HS256"
  access_token_expire_minutes: 30
  refresh_token_expire_days: 30
  default_admin_password: "CHANGE_ME"

cors:
  allowed_origins:
    - "https://your-domain.example"
